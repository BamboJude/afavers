{
  "name": "job-tracker",
  "version": "1.0.0",
  "type": "module",
  "description": "Full-stack job tracking platform",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build:client": "npm run build --workspace=client",
    "install:server": "npm install --workspace=server",
    "install:client": "npm install --workspace=client"
  },
  "keywords": [
    "job-tracker",
    "job-search",
    "react",
    "nodejs",
    "postgresql"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
